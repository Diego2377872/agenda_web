{
  "name": "agenda_web",
  "version": "1.0.0",
  "main": "index.html",  // Punto de entrada (opcional para Netlify)
  "scripts": {
    "start": "netlify dev",  // Para probar localmente
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@octokit/rest": "^19.0.7",  // Versión estable para GitHub API
    "dotenv": "^16.0.3"          // Para variables de entorno en desarrollo
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"     // Para ejecutar Netlify localmente
  }
}